<Window x:Class="ExcelDropViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Title="Excel 드롭 뷰어"
        Icon="AppExcelViewer_K3.ico"
        WindowState="Maximized"
        Background="#F5F6F8">
    <Window.Resources>
        <Style x:Key="ZoneDropHintStyle" TargetType="TextBlock">
            <Setter Property="Text" Value="엑셀 파일을 여기로 드래그 앤 드롭하세요." />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Foreground" Value="#6B7280" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="16" />
        </Style>
        <Style x:Key="DropZoneBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#FAFBFC" />
            <Setter Property="BorderBrush" Value="#C5CAD3" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Margin" Value="4" />
        </Style>
        <Style x:Key="ZoneReoGridHostStyle" TargetType="wfi:WindowsFormsHost">
            <Setter Property="Visibility" Value="Collapsed" />
            <Setter Property="Margin" Value="12" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="160" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="White" BorderBrush="#D1D5DB" BorderThickness="0,0,0,1">
            <MenuItem Header="_File">
                <MenuItem Header="Save _As..." Click="SaveAsMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="Tool">
                <MenuItem Header="BOM one row" Click="BomOneRowMenuItem_Click" />
                <MenuItem Header="Compare BOM" Click="CompareBomMenuItem_Click" />
                <MenuItem Header="Reference check" Click="ReferenceCheckMenuItem_Click" />
                <MenuItem Header="Make BOM DB" Click="MakeBomDbMenuItem_Click" />
                <MenuItem Header="Make PBA BOM" Click="MakePbaBomMenuItem_Click" />
                <MenuItem Header="Match PDB BOM with DB" Click="MatchPdbBomWithDbMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="설정">
                <MenuItem Header="Digikey" Click="MenuDigikeyConfig_Click" />
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Margin="12" Background="#F5F6F8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="240" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="240" />
            </Grid.ColumnDefinitions>

            <Border x:Name="LeftZone"
                    Grid.Column="0"
                    Style="{StaticResource DropZoneBorderStyle}"
                    AllowDrop="True"
                    DragOver="LeftZone_DragOver"
                    Drop="LeftZone_Drop">
                <Grid>
                    <TextBlock x:Name="LeftDropHint" Style="{StaticResource ZoneDropHintStyle}" />
                    <wfi:WindowsFormsHost x:Name="LeftReoGridHost" Style="{StaticResource ZoneReoGridHostStyle}" />
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="6"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Background="#D1D5DB"
                          ShowsPreview="True" />

            <Border x:Name="RightZone"
                    Grid.Column="2"
                    Style="{StaticResource DropZoneBorderStyle}"
                    AllowDrop="True"
                    DragOver="RightZone_DragOver"
                    Drop="RightZone_Drop">
                <Grid>
                    <TextBlock x:Name="RightDropHint" Style="{StaticResource ZoneDropHintStyle}" />
                    <wfi:WindowsFormsHost x:Name="RightReoGridHost" Style="{StaticResource ZoneReoGridHostStyle}" />
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Background="#FAFBFC"
                BorderBrush="#D1D5DB"
                BorderThickness="0,1,0,0">
            <DockPanel Margin="8">
                <TextBlock DockPanel.Dock="Top"
                           Text="Log"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="#374151"
                           Margin="0,0,0,6" />
                <ScrollViewer x:Name="LogScrollViewer"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <TextBox x:Name="LogTextBox"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             BorderThickness="0"
                             Background="Transparent"
                             FontFamily="Consolas"
                             FontSize="12"
                             Foreground="#1F2937"
                             VerticalScrollBarVisibility="Disabled"
                             HorizontalScrollBarVisibility="Disabled" />
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</Window>
